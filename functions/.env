# Environment variables for Firebase Functions
# These replace the deprecated functions.config() API

# Rate limiting configuration
RIVOCT_RATE_LIMIT_MINUTE=5
RIVOCT_RATE_LIMIT_DAY=100
RIVOCT_RATE_LIMIT_MONTH=1000

# Pricing configuration (client-facing price)
RIVOCT_PRICING_PER_CALL=0.32

